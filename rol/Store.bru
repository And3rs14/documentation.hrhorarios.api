meta {
  name: Store
  type: http
  seq: 1
}

post {
  url: {{base_url}}/roles
  body: json
  auth: bearer
}

auth:bearer {
  token: Bearer {{access_token}}
}

body:json {
  {
    "name": "editor"
  }
  
}
