meta {
  name: Edit
  type: http
  seq: 4
}

put {
  url: {{base_url}}/v1/roles/5
  body: json
  auth: bearer
}

auth:bearer {
  token: Bearer {{access_token}}
}

body:json {
  {
    "name": "jefe"
  }
  
}
