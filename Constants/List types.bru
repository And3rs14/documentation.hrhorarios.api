meta {
  name: List types
  type: http
  seq: 2
}

get {
  url: {{base_url}}/v1/constants?type=hola
  body: none
  auth: bearer
}

params:query {
  type: hola
}

auth:bearer {
  token: Bearer {{access_token}}
}
