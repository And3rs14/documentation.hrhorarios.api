meta {
  name: List
  type: http
  seq: 1
}

get {
  url: {{base_url}}/personal/search?search=pe
  body: none
  auth: bearer
}

params:query {
  search: pe
}

auth:bearer {
  token: Bearer {{access_token}}
}
