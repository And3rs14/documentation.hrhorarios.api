meta {
  name: Assign Roles
  type: http
  seq: 3
}

put {
  url: {{base_url}}/v1/users/7/roles
  body: json
  auth: bearer
}

auth:bearer {
  token: Bearer {{access_token}}
}

body:json {
  {
    "roles": ["Supervisor"]
  }
}
