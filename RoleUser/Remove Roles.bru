meta {
  name: Remove Roles
  type: http
  seq: 2
}

delete {
  url: {{base_url}}/v1/users/1/roles
  body: json
  auth: bearer
}

auth:bearer {
  token: Bearer {{access_token}}
}

body:json {
  {
    "roles": ["Supervisor"]
  }
}
